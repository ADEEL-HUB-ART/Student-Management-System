## Setup

1. Install de```

2. Set environment variables:
```bash
cp .env.example .env
# Edit .env with your values
```

3. Run the application:
```bash
uvicorn main:app --reload
```

## API Endpoints

- `POST /signup` - Register new user
- `POST /login` - Login and get token
- `GET /students/` - List all students (protected)
- `POST /students/` - Create student (protected)
- `GET /students/{id}` - Get student by ID (protected)
- `PUT /students/{id}` - Update student (protected)
- `DELETE /students/{id}` - Delete student (protected)

## Authentication

Use the token from `/login` in the Authorization header:
```
Authorization: Bearer <your-token>
```
=======
Student Management System (Backend API)

This project is a complete backend system for managing academic and administrative data of a school or university. It is built using FastAPI, SQLAlchemy, and PostgreSQL, and secured with JWT authentication and role-based access control (Admin, Teacher, Student).

The system allows institutions to manage students, departments, subjects, academic results, fees, and clearance status in a structured and secure way. It also provides analytical features such as GPA/CGPA calculation and an admin dashboard for system-wide statistics.

✨ Key Features

🔐 JWT-based Authentication (Signup & Login)

👥 Role-Based Access Control (Admin / Teacher / Student)

🎓 Student, Department, and Subject Management

📝 Result Management (Midterm, Final, Sessional, Total Marks)

📊 GPA and CGPA Calculation

💰 Fee Management (Paid, Due, Status Tracking)

🧾 Student Clearance System (Library, Finance, Hostel, Department)

📈 Admin Dashboard with system statistics

🗄️ PostgreSQL Database with SQLAlchemy ORM

📦 RESTful API with proper validation using Pydantic

🛠 Tech Stack

Backend: FastAPI

Database: PostgreSQL

ORM: SQLAlchemy

Authentication: JWT

Validation: Pydantic

🎯 Purpose

This project demonstrates how a real-world university or school management backend can be designed using modern Python frameworks. It is suitable for learning, academic projects, and as a foundation for building a full-stack student portal.
>>>>>>> b9ad6a472f73db801f84ce61fea808fd67331fc4
